1 what is packeges 

packages  = packages is the code which is written by another developer and we can access and use it in our code base

2 why do we need packages
we need packages to avoid reinventing the wheel , instead of writing code from scratch we can use packages which are already written and tested by other developers

3 how to install packages
we can install packages using npm (node package manager)

4 command to install packages
npm install package_name  = it will install the package and create a node_modules folder and package-lock.json file\
5 command to create package.json file

  
npm init -y  = it will create a package.json file with default values

npm i package name = krke package ko isntall krlo

jo file me uss package ko use krna hai usme require('packages_name ') krke use kr skte ho

6 what is server
 server is a machine jiske pass khud ka operating system khud ka storage or ram hai 
 
 iss machine ko ess tarah se program iya jata hai ki user jo bhi request bheje uska response de de
 
  